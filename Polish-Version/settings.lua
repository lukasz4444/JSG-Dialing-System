local settings = {
    usedhd = false , --czy chcesz wybrać adres za pomocą dhd (Ostrzeżenie jest to funkcja eksperymentalna).
    justone = false , -- tylko jeden obiekt może przejść przez gwiezdne wrota, po czym się wyłączą.
    dorestart = true -- Restartować?
}

return settings