local settings = {
    usedhd = false , --do you want to dial with dhd (Warning this is experimental feature.)
    justone = false , -- only one object can go through stargate after that they will disengage.
    dorestart = true -- restart after aborting dial or disengage of gate?
}

return settings